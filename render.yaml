services:
  - type: web
    name: retailerdisplay-api
    runtime: dotnet
    plan: free
    rootDir: src/RetailerDisplay.Api
    buildCommand: dotnet publish -c Release -o ./publish
    startCommand: dotnet ./publish/RetailerDisplay.Api.dll